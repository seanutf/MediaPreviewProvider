package com.seanutf.mediapreviewprovider.data;


public class ImageEditConstant {
    /**
     * 默认图片裁剪参数
     * 宽750
     * 高750
     */
    public static final int DEFAULT_IMG_EDIT_WIDTH = 750;
    public static final int DEFAULT_IMG_EDIT_HEIGHT = 750;

    /**
     * 具体业务在这下面进行宽高的单列
     * 不要修改默认值
     * 如果与默认值相同
     * 可以直接使用默认值，或用默认值赋值，如
     * IMG_EDIT_WIDTH_模块名或业务名 = 680
     * IMG_EDIT_HEIGHT_模块名或业务名 = 960
     */
}
